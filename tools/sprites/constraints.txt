# Exact versions the committed atlas was verified against (Python 3.11). CI installs with these so the
# atlas-reproduction check does not drift when a new numpy/Pillow/SciPy release quantises a pixel differently.
numpy==2.4.6
Pillow==12.3.0
scipy==1.17.1
