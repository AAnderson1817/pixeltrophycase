# Sprite pipeline (tools/sprites/extract.py). Install with the pins so the output matches the committed atlas byte for
# byte (CI checks this):  pip install -r tools/sprites/requirements.txt -c tools/sprites/constraints.txt
numpy>=2.0
Pillow>=11.0
scipy>=1.13
